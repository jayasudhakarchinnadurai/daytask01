1.Write difference between HTTP/1.1 and HTTP/2

HTTP/1.1                                               HTTP/2
Ithe usest works on the textual format.                It works on the binary protocol.

There is head of line blocking that blocks             It allows multiplexing so one TCP
all the requests behind it until it doesn’t            connection is required for multiple
get its all resources.                                 requests.

It uses requests resource Inlining for use             It uses PUSH frame by server that
It compresses data by itself.                          It uses HPACK for data compression


2.write a blog about objects and internal representation in javascript

In JavaScript, an object is a standalone entity, with properties and type. 
Compare it with a cup, for example. A cup is an object, with properties. 
A cup has a color, a design, weight, a material it is made of, etc. The same 
way, JavaScript objects can have properties, which define their characteristics.
